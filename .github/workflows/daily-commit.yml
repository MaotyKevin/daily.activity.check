name: Daily Auto Commit

on:
  schedule:
    - cron: '5 23 * * *'   # 02:05 AM UTC+3
    - cron: '0 6 * * *'    # 09:00 AM UTC+3
    - cron: '0 11 * * *'   # 02:00 PM UTC+3
    - cron: '0 17 * * *'   # 08:00 PM UTC+3
  workflow_dispatch:

permissions:
  contents: write

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update activity log
        run: |
          echo "Auto-commit: $(date '+%Y-%m-%d at %H:%M UTC')" >> activity_log.txt

      - name: Commit and push
        run: |
          git config user.name "MaotyKevin"
          git config user.email "maotykevin@gmail.com"
          git add activity_log.txt
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: daily activity update $(date +'%Y-%m-%d %H:%M')" && git push)